{
  "name": "test-deploy",
  "version": "1.0.0",
  "description": "Test deployment for AI Development Platform",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:integration": "jest --testMatch='**/*.itest.ts'",
    "test:unit": "jest --testMatch='**/*.test.ts'",
    "test:coverage": "jest --coverage",
    "build": "tsc",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "fs-extra": "^11.1.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/jest": "^29.5.0",
    "@types/node": "^18.15.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}