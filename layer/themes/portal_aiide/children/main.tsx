import React from 'react';
import ReactDOM from 'react-dom/client';
import { AIIDEApp } from './AIIDEApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AIIDEApp />
  </React.StrictMode>,
);
