# Database Configuration
DB_TYPE=sqlite  # Options: sqlite, postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=ai_dev_portal

# SQLite specific (if DB_TYPE=sqlite)
SQLITE_PATH=./data/ai_dev_portal.db

# PostgreSQL connection string (alternative to individual settings)
# DATABASE_URL=postgresql://user:password@localhost:5432/ai_dev_portal

# Server Configuration
PORT=3456
JWT_SECRET=your-secret-key-change-in-production

# Environment
NODE_ENV=development