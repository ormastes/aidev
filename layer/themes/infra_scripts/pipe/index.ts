/**
 * Infrastructure Scripts Theme - Public API Gateway
 * 
 * This module exports the public interface for script utilities,
 * following the Hierarchical Encapsulation Architecture (HEA) principles.
 */

// Export script utilities when they become modules
export {};

/**
 * Note: Most scripts are standalone executables.
 * This pipe/index.ts exists to satisfy HEA requirements.
 */