{
  "name": "026-auto-env-generation",
  "version": "1.0.0",
  "description": "Automatic .env file generation for each environment and service",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "environment",
    "configuration",
    "env",
    "token",
    "security"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}