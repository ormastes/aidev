{
  "name": "@aidev/infra_story-reporter",
  "version": "1.0.0",
  "description": "Story reporting and analysis tools for aidev",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --target node --outdir dist",
    "dev": "bun run src/cli/coverage-analyzer.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "test:jest": "jest",
    "test:jest:coverage": "jest --coverage"
  },
  "dependencies": {
    "glob": "^8.1.0"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "jest": "^29.5.0",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}